import { SupplierChat } from "@/components/chat/SupplierChat"

// Optional layout override if needed, but returning full height component
export default function SupplierChatPage() {
  return <SupplierChat />
}
